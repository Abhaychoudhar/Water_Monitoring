<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ThingSpeak IoT Data</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

  <style>
    /* Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1e3c72, #2a5298, #1e3c72);
      overflow: hidden;
    }

    .content-wrapper {
      height: 100%;
      overflow-y: auto;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
    }

    .container {
      width: 90%;
      max-width: 800px;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      color: #fff;
      text-align: center;
    }

    h1, h2 {
      color: #fff;
      margin-bottom: 10px;
    }

    p {
      font-size: 1.2rem;
      color: rgba(255, 255, 255, 0.8);
      margin-bottom: 20px;
    }

    a {
      display: inline-block;
      margin-top: 10px;
      padding: 10px 20px;
      font-size: 1rem;
      color: #1e3c72;
      background-color: #fff;
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      transition: all 0.3s;
    }

    a:hover {
      background-color: #1e3c72;
      color: #fff;
      transform: scale(1.05);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4);
    }

    .table-container {
      width: 100%;
      max-width: 800px;
      overflow-y: auto;
      max-height: 400px;
      border-radius: 12px;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
      background-color: rgba(255, 255, 255, 0.1);
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      padding: 15px;
      text-align: center;
      color: #fff;
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    th {
      background: linear-gradient(45deg, #4facfe, #00f2fe);
      position: sticky;
      top: 0;
      z-index: 2;
    }

    tr:nth-child(even) {
      background-color: rgba(255, 255, 255, 0.1);
    }

    tr:hover {
      background-color: rgba(255, 255, 255, 0.3);
      transform: scale(1.02);
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 1.8rem;
      }

      p {
        font-size: 1rem;
      }

      th, td {
        padding: 10px;
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <div class="content-wrapper">
    <div class="container">
      <h1>Don't Throw It</h1>
      <p>Realtime data from various Water Bodies</p>
      <a href="/thingspeak/alternate_uses">Go to Alternate Options</a>
    </div>

    <h1>Real-Time IoT Data from ThingSpeak</h1>
    <h2>Channel: {{channelInfo.name}}</h2>
    <p>{{channelInfo.description}}</p>

    <div class="table-container" id="table-container">
      <table>
        <thead>
          <tr>
            <th>Timestamp</th>
            <th>pH</th>
            <th>Dissolved Oxygen</th>
            <th>Turbidity</th>
            <th>Temperature</th>
          </tr>
        </thead>
        <tbody>
          {{#each feeds}}
          <tr>
            <td>{{created_at}}</td>
            <td>{{field1}}</td>
            <td>{{field2}}</td>
            <td>{{field3}}</td>
            <td>{{field4}}</td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

  <script>
    // Scroll to the bottom of the table when the page loads
    window.onload = () => {
      const tableContainer = document.getElementById('table-container');
      tableContainer.scrollTop = tableContainer.scrollHeight;
    };

    // Auto-refresh the page every 5 seconds
    setInterval(() => {
      window.location.reload();
    }, 5000);
  </script>
</body>
</html>
